# v0.2 scoped push 指令（Yao 已授权 · 2026-06-25）

> Cowork 复核通过（反膨胀=0、validator 10/10、降级保守正确）。下面整段粘进办公室 CC。
> 关键：工作树有大面积 CRLF/mode 漂移，**必须用白名单 + 守卫**，不要 git add -A。

```
BMDL v0.2 — scoped push（Yao 已授权）。不要 git add -A，不要 force-push，不要改 history。

0) 关掉 mode-bit 噪声：git config core.fileMode false

1) 只 add 这些白名单路径（真实交付物）：
   git add prototypes_db/ pollutant_profiles.json pollutant_aliases.json feature_matching_rules.json
   git add examples/adrmats_briefs/
   git add tools/biomimetic_context.py tools/generate_adrmats_briefs.py tools/check_brief_mechanism_binding.py tools/check_translation_specificity.py tools/check_dt_actionability_36.py tools/check_gold_set_usefulness.py tools/check_brief_do_not_behavior.py tools/export_do_not.py
   git add docs/active/v0.2-gold-set.json docs/active/v0.2-gold-set.md docs/active/emerging-pollutants-20.json docs/active/emerging-pollutants-20.md docs/active/emerging-pollutants-20-source.xls docs/active/adrmats-dogfood-scorecard.md docs/active/v0.2-acceptance-plan-officecc-20260625.md docs/active/HANDOFF-office-to-home-20260625.md docs/active/acceptance/
   git add docs/active/execution-state.json   # 仅当 CC 这次更新过它；没动就跳过

2) 守卫：确认没有受保护/噪声文件被 staged。运行：
   git diff --cached --name-only | grep -E '\.claude/settings\.local|\.gitmodules|\.gitignore|tools/litextract|docs/optimization-v1|^runtime/|docs/archive/|docs/imported/|docs/registries/|prototypes/.*\.md'
   若有输出 → git restore --staged <这些路径> 全部取消暂存，直到上面 grep 无输出。

3) 自检 staged 内容：
   git diff --cached --stat | tail -5
   # 确认只含 prototypes_db / pollutant_*/ tools/ examples/adrmats_briefs / docs/active 交付物。

4) 提交 + 推送（普通 push，非 force）：
   git commit -m "feat(v0.2): evidence honesty fix (149 downgrades, 0 inflation) + ADRMATS brief honesty/lane/DO-NOT gate + Phase E 21 emerging-pollutant profiles/gold-set/backlog + 2 validator bugfixes; G1-G8 green, anti-inflation verified"
   git push origin review

5) push 后：把新 HEAD sha 回填到 docs/active/HANDOFF-office-to-home-20260625.md §1，并把 sha 贴给我确认。
   stash@{0} 保持不动（DQ-6 未决，勿 pop/drop）。
```

## 守卫口径（出问题就停、写 decision-queue）

- 若 grep 守卫反复命中、无法清成 0 staged 受保护文件 → 停下，写 decision-queue，别强行 commit。
- 若 `git push` 报 non-fast-forward（远端有新 commit）→ 停下问 Yao，别 force。
- 提交信息可微调，但必须保留 "0 inflation / G1-G8" 关键词以便追溯。
