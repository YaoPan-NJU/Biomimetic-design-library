# v0.2 验收 — Yao 批决策指令（2026-06-25 第 1 批）

> 由 Yao 在 review 后拍板，解决 decision-queue 的 DQ-1/DQ-4/DQ-6 + 补完 G5/G2。
> 决策结果：DQ-1=诚实方向修复；DQ-4=改验证器+确认后要求变绿；DQ-6=先看 diff 再逐条应用、保留 stash；收尾=现在补完 G5/G2 再考虑 push。
> 下面整段可直接粘进办公室 Claude Code（ultracode / auto 模式）。

```
BMDL v0.2 — Yao 批决策指令（2026-06-25 第1批）。继续 GOAL/auto 模式：链式做完 A→D，新出现的需我决策项写进 docs/active/acceptance/v0.2-decision-queue-20260625.md 不停机；全部 DONE 或剩余项 deferred 后，再一次性给我更新后的 G1–G8 gate 表。默认 mimo-v2.5-pro；仅当需要读扫描/图片页时切 mimo-v2.5。所有 prototype_db 改动先 dry-run + reviewer 抽检；不 push（等我授权）。
建议顺序：先 B、C、D-G5（都快），再用子代理跑 A（长项，2 worker + 1 reviewer，守 ≤3 并发）。

Task A（DQ-1 诚实方向修证据）：
1) 列出 41 个非法 basis + 111 个缺 locator 元素（来自 check_boundary_guardrail / check_from_source_integrity / check_fact_requires_locator），含 prototype_id、field_path、当前 basis/verification、source_doi、source_file。
2) 逐条尝试从引用的本地 PDF 接地：四要素（source_doi + 真实 quote ≤200 字 + 精确 locator + scope_match 2+ 关键词）且过五问测试 → 保留/升级为 from_source（带真 quote+locator）。
3) 接不到的（无 PDF / 无匹配引文 / scope 不符）→ 降级为 literature_backed / needs_review / missing_pdf。严禁编造 locator，严禁为凑绿把 literature_backed 批量刷成 from_source。
4) 每条改动先 dry-run；reviewer 按风险抽检后才写。完成后重跑这三个 validator，目标变绿（或诚实减少并记录残留）。
5) 读文本 PDF 用 mimo-v2.5-pro；遇扫描/图片页才切 mimo-v2.5。产出 docs/active/audit-candidates/cc-dq1-evidence-fix-20260625.md。

Task B（DQ-4 修绑定验证器）：
1) 先列出 5 个被判绑定问题的候选：query、展示的机制、命中的特征、为何是假阳（命中自由描述文本而非结构化字段）。
2) 改 tools/check_brief_mechanism_binding.py：query 特征只比对结构化字段 molecular_feature_links/functional_groups，不比对自由描述文本。
3) 重跑，要求 G2 对 7 个 brief 全绿。若 5 个里有真绑定错（非假阳）→ 改 adapter 绑定逻辑，不要 whitelist。

Task C（DQ-6 看 stash 再逐条应用）：
1) git stash show -p stash@{0} 出 diff，列出涉及的 prototype_db 文件与字段（scope caveats / metadata fixes）。
2) 逐 hunk 判断是否已在当前 origin/review HEAD：已存在或已过时 → 跳过；未合进且仍正确 → dry-run 后选择性应用。
3) 不整体 pop、不 drop stash，保留 stash@{0}。歧义 hunk 写 decision-queue。产出 docs/active/acceptance/dq6-stash-review-20260625.md，逐 hunk 标 applied/skipped-present/skipped-stale/needs-yao。

Task D（补完 G5 + G2）：
1) G5：Phase 3 改 brief 后重跑/重评 dogfood（docs/active/adrmats-dogfood-scorecard.md），确认有机域候选 boundary_usefulness>0、honesty_ledger>0、均分 >3.0；记录前后对比。脚本就跑，手填按 rubric 重评。
2) G2：Task B 后确认 check_brief_mechanism_binding 全绿。

收尾：更新 docs/active/acceptance/v0.2-acceptance-20260625.md 与 decision-queue，重写 REVIEW_REQUEST；不 push，等我授权。完成后给我更新后的 G1–G8 gate 表。
```

## 验收口径（CC 自检 + Yao 复核用）

- DQ-1 **只认诚实方向**：from_source 数量**允许下降**（降级是诚实的）；绝不接受"靠把 literature_backed 刷成 from_source 让 validator 变绿"。reviewer 必须复现每条升级的 quote+locator。
- DQ-4 真正成功 = G2（check_brief_mechanism_binding）对 7 个 brief **0 报错**，且 5 个案例确属假阳（或真 bug 已在 adapter 修掉）。
- DQ-6 **stash 必须保留**到 Yao 确认；逐 hunk 处置可审计。
- G5 成功 = dogfood 有机域候选 BU>0 且 HLC>0、均分 >3.0，有前后对比。
- 全程不 push。
