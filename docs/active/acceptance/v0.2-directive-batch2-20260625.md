# v0.2 验收 — Yao 批决策指令（2026-06-25 第 2 批 · 收尾）

> 决策：DQ-4=scope_mismatch 优先（有真来源才绑）；收尾=修完两处→Cowork 复核关键 diff→Yao 授权后再 push。
> 下面整段可直接粘进办公室 Claude Code（ultracode / auto 模式）。**做完停在 push 前，不要 push。**

```
BMDL v0.2 — Yao 批决策指令（2026-06-25 第2批·收尾）。继续 GOAL/auto 模式，链式做完 A→B→C。默认 mimo-v2.5-pro；仅当为查 silk-fibroin↔染料来源需读扫描/图片页时切 mimo-v2.5。改 prototype_db 前先 dry-run。本批做完**停在 push 前，不要 push**，等 Yao 复核后授权。

Task A（DQ-4 真绑定错 silk-fibroin↔亚甲基蓝，scope_mismatch 优先）：
1) 先在 silk-fibroin 的本地来源 PDF 里查有没有“真有来源”的 silk-fibroin↔阳离子染料/亚甲基蓝 机制（-COO⁻ 静电 + 芳香 π-π），需过四要素（source_doi+真实 quote≤200字+精确 locator+scope_match 2+关键词）+五问。
2) 找到 → 把该候选绑定到那条真机制（带真 quote+locator）。找不到 → 标 scope_mismatch、把 silk-fibroin 移出 MB 查询的 fact/lead 道。严禁凭空加 MB 机制。dry-run 后写。
3) 重跑 tools/check_brief_mechanism_binding.py，要求 G2 对 7 个 brief 全绿（7/7）。15 个空字段是 warning，可不动。

Task B（G1 microbial-exopolysaccharide 缺 DT）：
1) 给该原型补一条可执行 design_translation：idea + material_realization_examples/handle + source_tier，按证据如实标 tier（无来源就 exploratory/llm_inferred，但必须 actionable）。dry-run 后写。
2) 重跑 tools/check_dt_actionability_36.py 与 §2.5 数据层，要求 G1 该项通过（目标 10/10 或诚实记录残留）。

Task C（产出复核摘要 + 收尾，不 push）：
1) 写 docs/active/acceptance/v0.2-change-digest-20260625.md，包含：
   - from_source 本批前/后数量与占比（/2080）；
   - 149 个降级清单按原型+原因分组（108 个 from_source 无 quote→llm_inferred；41 个非法 basis→llm_inferred；1 个 hard DO-NOT→soft）；
   - **反膨胀确认**：本批 literature_backed→from_source 升级数 = 0（必须为 0）；
   - DQ-4 处置结论（绑定到真机制 or scope_mismatch）+ microbial DT 新增内容；
   - 最终 G1–G8 gate 表。
2) 更新 v0.2-acceptance-20260625.md 与 decision-queue，重写 REVIEW_REQUEST。
3) **停在 push 前**，等 Yao 授权。完成后把 change-digest 的要点回贴给我。
```

## Cowork 复核口径（Yao 授权 push 前我会核这几条）

- from_source **允许下降**（降级=诚实）；核对 digest 的前/后数与占比合理。
- **反膨胀硬条件**：本批 literature_backed→from_source = 0。若 >0，必须逐条查证后才接受。
- DQ-4：silk-fibroin↔MB 要么绑定到**可复现 quote+locator** 的真机制，要么 scope_mismatch；不接受凭空 MB 机制。
- G1/G2：microbial DT 已补、check_brief_mechanism_binding 7/7、check_dt_actionability 通过。
- 抽查若干降级元素的 JSON diff，确认是“降级/标注”而非删数据或改语义。
- 确认 stash@{0} 仍保留（DQ-6 未丢）。
