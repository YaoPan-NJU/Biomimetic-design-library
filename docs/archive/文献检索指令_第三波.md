# 文献检索指令（第三波 · 精简版）

> 日期：2026-06-07
> 目标：补充 8 个原型的文献，约 60 篇

---

## 背景

前两波 311 个 JSON 已生成 33 个原型，但有 8 个原型数据不足或空白：

| 原型 | 状态 | 需要 |
|---|---|---|
| mussel-foot-adhesion | 3 条 perf（chimera 修复后） | 补充贻贝配位吸附数据 |
| dna-aptamer | 0 数据 | 建立基础 |
| biomineralization-template | 0 数据 | 建立基础 |
| silkworm-silk | 0 数据 | 建立基础 |
| diatom-inspired-porous | 0 数据 | 建立基础 |
| iron-oxidizing-bacteria | 0 数据 | 建立基础 |
| plant-tannin | 0 数据 | 建立基础 |
| scallop-shell | 0 数据 | 建立基础 |

---

## 检索组（4 组，共 ~60 篇）

### 第 A 组：贻贝仿生吸附（~10 篇）

**覆盖原型**：mussel-foot-adhesion

**英文检索式（WoS）**：
```
TS=("mussel-inspired" OR "polydopamine" OR "catechol" OR "DOPA" OR "polyphenol" OR "tannic acid") 
AND 
TS=("adsorption" OR "heavy metal" OR "pollutant" OR "chelation" OR "metal ion removal") 
AND NOT TS=("superhydrophobic" OR "oil-water separation" OR "self-cleaning" OR "antifouling")
```

**中文检索（CNKI）**：
```
(仿贻贝 OR 多巴胺 OR 邻苯二酚 OR 聚多巴胺) AND (吸附 OR 重金属 OR 配位 OR 水处理) 
AND NOT (超疏水 OR 油水分离 OR 防污)
```

**目标**：10 篇，优先有 qmax 数据的研究论文。

---

### 第 B 组：新方向建立（~25 篇）

**覆盖原型**：dna-aptamer、biomineralization-template、silkworm-silk

**英文检索式（WoS）**：

**B1 - DNA 适配体（8 篇）**：
```
TS=("DNA aptamer" OR "nucleic acid aptamer" OR "aptamer-functionalized") 
AND 
TS=("heavy metal" OR "lead" OR "mercury" OR "cadmium" OR "water" OR "wastewater")
```

**B2 - 生物矿化模板（8 篇）**：
```
TS=("biotemplate" OR "biological template" OR "shell-derived" OR "nacre-inspired" OR "biomineralization") 
AND 
TS=("adsorbent" OR "adsorption" OR "heavy metal" OR "nanomaterial")
AND NOT TS=("fossil" OR "archaeology")
```

**B3 - 蚕丝蛋白（5 篇）**：
```
TS=("silk fibroin" OR "silkworm" OR "Bombyx mori") 
AND 
TS=("adsorption" OR "heavy metal" OR "dye" OR "water treatment")
```

**中文检索（CNKI）**：
```
(DNA适配体 OR 适配体) AND (重金属 OR 吸附 OR 水处理)
```
```
(生物矿化 OR 生物模板 OR 贝壳) AND (吸附剂 OR 纳米材料)
```
```
(蚕丝蛋白 OR 丝素蛋白) AND (吸附 OR 重金属 OR 水处理)
```

---

### 第 C 组：零数据原型补全（~15 篇）

**覆盖原型**：diatom-inspired-porous、iron-oxidizing-bacteria、plant-tannin、scallop-shell

每个原型 3-4 篇核心文献：

**C1 - 仿硅藻多孔材料（4 篇）**：
```
TS=("diatom-inspired" OR "diatomaceous earth" OR "diatomite" OR "diatom-based") 
AND 
TS=("adsorption" OR "heavy metal" OR "dye" OR "water treatment")
```

**C2 - 铁氧化细菌（4 篇）**：
```
TS=("iron-oxidizing bacteria" OR "ferrihydrite" OR "schwertmannite" OR "biogenic iron oxide") 
AND 
TS=("heavy metal" OR "arsenic" OR "adsorption" OR "acid mine drainage")
```

**C3 - 植物单宁（4 篇）**：
```
TS=("tannin" OR "tannic acid" OR "plant polyphenol" OR "condensed tannin") 
AND 
TS=("adsorption" OR "heavy metal" OR "dye" OR "water treatment")
AND NOT TS=("food" OR "wine" OR "leather" OR "health")
```

**C4 - 扇贝壳（3 篇）**：
```
TS=("scallop shell" OR "shell waste" OR "bivalve shell") 
AND 
TS=("adsorption" OR "heavy metal" OR "phosphate" OR "water treatment")
```

**中文检索（CNKI）**：
```
(硅藻土 OR 仿硅藻) AND (吸附 OR 重金属 OR 水处理)
```
```
(铁氧化细菌 OR 施氏矿物) AND (重金属 OR 吸附)
```
```
(单宁 OR 植物多酚) AND (吸附 OR 重金属 OR 水处理) AND NOT (食品 OR 皮革)
```
```
(扇贝壳 OR 贝壳废弃物) AND (吸附 OR 重金属 OR 水处理)
```

---

### 第 D 组：跨原型补充（~10 篇）

**覆盖**：再生循环数据（所有原型都需要）

```
TS=("regeneration" OR "reusability" OR "recyclable adsorbent" OR "cyclic performance") 
AND 
TS=("polydopamine" OR "chitosan" OR "alginate" OR "MOF" OR "hydrogel") 
AND 
TS=("heavy metal" OR "dye" OR "water")
```

**中文检索（CNKI）**：
```
(再生 OR 循环利用 OR 重复使用) AND (仿生吸附 OR 壳聚糖 OR 聚多巴胺) AND (重金属 OR 染料)
```

**目标**：10 篇，覆盖前两波缺少的再生循环数据。

---

## 专利检索（~10 篇）

```
(DNA适配体 OR 仿贻贝 OR 聚多巴胺 OR 生物矿化) AND (吸附剂) AND (重金属 OR 水处理)
AND NOT (超疏水 OR 油水分离)
```

---

## 文件夹结构

```
文献库/论文/
├── 第A组-贻贝仿生/
├── 第B组-新方向/
├── 第C组-零数据原型/
└── 第D组-再生循环/
```

---

## 总量

| 组 | 篇数 |
|----|------|
| A 组（贻贝） | ~10 |
| B 组（新方向） | ~25 |
| C 组（零数据） | ~15 |
| D 组（再生） | ~10 |
| 专利 | ~10 |
| **总计** | **~70** |

前两波 311 + 第三波 70 ≈ **380 篇**。

---

*本文件由 Claude Code 生成于 2026-06-07*
